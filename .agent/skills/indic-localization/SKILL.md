# Indic Localization

All user-facing strings live in `/lib/i18n/{lang}.json`. Never hardcode strings in components. Default language Hindi; fall back to English. Add stubs for `te`, `ta`, `bn`, `mr`.
